{
  "name": "yuvava",
  "displayName": "Navigator",
  "description": "An AI pair-programming navigator that observes your changes and points out problems — without writing your code.",
  "version": "0.1.0",
  "license": "MIT",
  "publisher": "navigator",
  "private": true,
  "engines": {
    "vscode": "^1.90.0",
    "node": ">=20"
  },
  "categories": [
    "Linters",
    "Programming Languages"
  ],
  "main": "./out/src/vscode/extension.js",
  "activationEvents": [],
  "contributes": {
    "commands": [
      {
        "command": "navigator.reviewChanges",
        "title": "Review Current Changes",
        "category": "Navigator"
      },
      {
        "command": "navigator.clearObservations",
        "title": "Clear Observations",
        "category": "Navigator"
      },
      {
        "command": "navigator.whereToLook",
        "title": "Where Should I Look?",
        "category": "Navigator"
      },
      {
        "command": "navigator.recallName",
        "title": "What Was It Called?",
        "category": "Navigator"
      },
      {
        "command": "navigator.goDeeper",
        "title": "Go Deeper",
        "category": "Navigator"
      },
      {
        "command": "navigator.setApiKey",
        "title": "Set API Key",
        "category": "Navigator"
      },
      {
        "command": "navigator.clearApiKey",
        "title": "Clear API Key",
        "category": "Navigator"
      },
      {
        "command": "navigator.showLog",
        "title": "Show Log",
        "category": "Navigator"
      }
    ],
    "configuration": {
      "title": "Navigator",
      "properties": {
        "navigator.provider": {
          "type": "string",
          "enum": [
            "anthropic",
            "openai"
          ],
          "enumDescriptions": [
            "Claude, via the Anthropic API.",
            "GPT / Codex, via the OpenAI API."
          ],
          "default": "anthropic",
          "markdownDescription": "Which service performs the review. Both are held to the same contract: the same prompt, the same response schema, and the same guarantee that Navigator never writes code."
        },
        "navigator.model": {
          "type": "string",
          "default": "",
          "markdownDescription": "Model used for review. Leave empty for the provider's default (`claude-opus-5` for Anthropic, `gpt-5.1-codex-max` for OpenAI)."
        },
        "navigator.openaiBaseUrl": {
          "type": "string",
          "default": "",
          "markdownDescription": "An OpenAI-compatible endpoint to use instead of OpenAI itself — Groq, Cerebras, Ollama, LM Studio, OpenRouter. Setting it switches the request to `/chat/completions`, which is what those services implement.\n\nExamples: `https://api.groq.com/openai/v1`, `http://localhost:11434/v1`.\n\n**Your diff is sent to whatever you point this at.** Free tiers commonly train on the data they receive — check the provider's terms. Navigator is a personal learning tool and does not manage this for you."
        },
        "navigator.reviewIntensity": {
          "type": "string",
          "enum": [
            "silent",
            "normal",
            "strict"
          ],
          "default": "normal",
          "markdownDescription": "How much Navigator is allowed to speak up.\n\n- `silent`: clear correctness bugs only\n- `normal`: correctness, edge cases, risky design\n- `strict`: adds security, performance, complexity and maintainability\n\nNo intensity ever allows Navigator to generate code."
        },
        "navigator.effort": {
          "type": "string",
          "enum": [
            "",
            "low",
            "medium",
            "high",
            "xhigh",
            "max"
          ],
          "enumDescriptions": [
            "Whatever the model does by default.",
            "Least thinking, least cost, most likely to miss something.",
            "Less thinking than the default.",
            "The usual default for a reasoning model.",
            "More thinking than high (Anthropic only; sent as high to OpenAI).",
            "As much thinking as the model will do (Anthropic only; sent as high to OpenAI)."
          ],
          "default": "",
          "markdownDescription": "How hard the model thinks before answering. This is the cost dial: for a review the input is small and the thinking dominates, so a lower setting is cheaper and a higher one is slower.\n\nLeave it empty unless you have a reason. Nobody has measured what a lower setting costs in review quality, so the default is the model's own."
        },
        "navigator.diffBase": {
          "type": "string",
          "default": "HEAD",
          "markdownDescription": "Git revision the working tree is diffed against, e.g. `HEAD` or `origin/main`."
        },
        "navigator.includeUntracked": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Also review new files git is not tracking yet. Files ignored by `.gitignore` are never included, and Navigator reads them without ever writing to the index."
        },
        "navigator.maxDiffBytes": {
          "type": "number",
          "default": 200000,
          "minimum": 1000,
          "markdownDescription": "Diffs larger than this are not sent for review."
        },
        "navigator.maxObservations": {
          "type": "number",
          "default": 20,
          "minimum": 1,
          "markdownDescription": "Upper bound on observations reported for a single review."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -p ./ -w",
    "typecheck": "tsc -p ./ --noEmit",
    "lint": "eslint src test --max-warnings 0",
    "test": "npm run compile && node --test \"out/test/**/*.test.js\"",
    "verify": "npm run lint && npm run compile && node --test \"out/test/**/*.test.js\"",
    "eval": "npm run compile && node scripts/eval.mjs",
    "smoke": "npm run compile && node scripts/smoke.mjs",
    "test:host": "npm run compile && node scripts/test-host.mjs",
    "package": "npm run compile && vsce package -o yuvava.vsix",
    "install:local": "npm run verify && npm run package && code --install-extension yuvava.vsix --force && echo Installed. Run Developer: Reload Window in VS Code to activate it."
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@types/node": "^22.10.0",
    "@types/vscode": "^1.90.0",
    "@typescript-eslint/eslint-plugin": "^8.18.0",
    "@typescript-eslint/parser": "^8.18.0",
    "@vscode/test-electron": "^3.1.0",
    "@vscode/vsce": "^3.2.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.68.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.122.0",
    "openai": "^7.8.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/eastasann/yuvava.git"
  }
}
