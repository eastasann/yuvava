---
name: Feedback（実利用からの観測）
about: Navigator を実際に使って気づいたこと。次のループの材料になります。
title: "feedback: "
labels: feedback
---

<!--
⚠️ このリポジトリは public です。

レビュー対象になった実コードをそのまま貼らないでください。
業務コードや他プロジェクトの断片が公開され、取り消しが効きません。

残す価値があるのは「コード」ではなく「パターン」です。

  ✗ src/billing/invoice.ts:142 で「amount が null の可能性」と指摘された
    該当コード: const total = invoice.amount * rate; ...

  ✓ 誤検知: 呼び出し元で既にガードされている値に null 指摘が出る。
    関数境界をまたいだガードを追えていない。

下は実コードを一切含まないのに、prompt.ts のどこを直すべきか特定できます。
再現に最小限のコードが要る場合は、変数名も含めて作り直した例にしてください。
-->

## 何が起きたか

<!-- Navigator が何と言ったか。指摘文はそのまま引用して構いません（コードは除く） -->

## 期待していた挙動

<!-- 何と言ってほしかったか。あるいは黙っていてほしかったか -->

## 種別

- [ ] 誤検知 — 問題でないものを指摘した
- [ ] 見逃し — 拾ってほしいバグを黙って通した
- [ ] ノイズ — 正しいが、言う価値がなかった（SPEC §7）
- [ ] コード生成 — 修正コードを出そうとした（SPEC §16 違反。最優先）
- [ ] 誤配置 — 指摘の行が間違っていた
- [ ] 操作性 — 挙動そのものが邪魔だった
- [ ] その他

## 設定

- provider / model:
- reviewIntensity:
- 拡張機能のバージョン:
